<!-- 
<div class="root">
  <%- include('nav', {}); %>
  <% if (stonks.length) { %>
    <h1>Your Saved Stonks</h1>
    <ul>
      <% stonks.forEach(function(stonk){ %>
        <li><a href="/stonks/<%= stonk.ticker %>"><%= stonk.ticker %></a>: <span id="ticker-<%= stonk.ticker %>"></span></li>
        <% }); %>
      </ul>
      <% } %>
      <form action="/stonks" method="POST">
        <h3>Your ticket to the moon</h3>
        <% if (errors.length) { %>
          <% errors.forEach(function(error){ %>
            <p><%= error %></p>
            <% }); %>
            <% } %>
            <label for="">Stonk Ticker</label>
            <input type="text" name="tickerSymbol">
            <input type="submit">
          </form>
</div>
<script src="/js/stonks-page.js"></script>
<%- include('footer'); %> --> 

<%- include('header', {stylesheets: ['/css/stonks.css']}); %>
    <div class="root">
      <%- include('nav'); %>
      <div class="mainContent">
        <form class="form" action="/stonks" method="POST">
          <% if (errors.length) { %>
            <% errors.forEach(function(error){ %>
              <p class="tickerInputError"><%= error %></p>
            <% }); %>
          <% } %>
          <label class="tickerInputLabel" for="">add ticker</label>
          <input class="tickerInputBox" type="text" maxlength="5" name="tickerSymbol"/>
          <button class="tickerInputSubmit" type="submit">
            💎🙌🏻
          </button>
        </form>
        <div class="stonk-card-container">
          <% stonks.forEach(function(stonk){ %>
            <a href="/stonks/<%= stonk.ticker %>" class="stonk-card">
              <h2 class="ticker-symbol"><%= stonk.ticker %></h2>
              <div id="ticker-<%= stonk.ticker %>" class="ticker-price"></div>
            </a>
          <% }); %>
        </div>
      </div>
    </div>
<script src="/js/stonks-page.js"></script>
<%- include('footer'); %> 

